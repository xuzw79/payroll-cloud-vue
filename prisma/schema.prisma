generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum PayType {
  MONTHLY
  HOURLY
}

model Employee {
  id          String    @id @default(cuid())
  employeeNo  String    @unique
  name        String
  email       String?
  defaultDependentCount Int @default(0)
  payType     PayType   @default(MONTHLY)
  basePay     Int
  memo        String?
  isActive    Boolean   @default(true)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  payrolls    Payroll[]
}

model Payroll {
  id                       String   @id @default(cuid())
  employeeId               String
  period                   String
  workDays                 Decimal  @db.Decimal(8, 2)
  workHours                Decimal  @db.Decimal(8, 2)
  overtimeHours            Decimal  @db.Decimal(8, 2)
  allowance                Int      @default(0)
  fixedDeduction           Int      @default(0)
  overtimeRate             Decimal  @db.Decimal(5, 3)
  incomeTaxRate            Decimal  @db.Decimal(5, 4)
  socialInsuranceRate      Decimal  @db.Decimal(5, 4)
  employmentInsuranceRate  Decimal  @db.Decimal(5, 4)
  socialInsuranceEnrolled  Boolean  @default(true)
  socialInsuranceBaseAmount Int?
  regularPay               Int
  overtimePay              Int
  grossPay                 Int
  incomeTax                Int
  socialInsurance          Int
  employmentInsurance      Int
  totalDeduction           Int
  netPay                   Int
  fiscalYear               Int?
  dependentCount           Int      @default(0)
  taxableIncome            Int?
  emailedAt                DateTime?
  note                     String?
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
  employee                 Employee @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@unique([employeeId, period])
  @@index([period])
}

model CompanySetting {
  id                       String   @id @default("default")
  currentFiscalYear        Int?
  overtimeRate             Decimal  @db.Decimal(5, 3) @default(1.25)
  incomeTaxRate            Decimal  @db.Decimal(5, 4) @default(0.0300)
  socialInsuranceRate      Decimal  @db.Decimal(5, 4) @default(0.1500)
  employmentInsuranceRate  Decimal  @db.Decimal(5, 4) @default(0.0060)
  updatedAt                DateTime @updatedAt
}

model FiscalRate {
  id                       String   @id @default(cuid())
  fiscalYear               Int      @unique
  overtimeRate             Decimal  @db.Decimal(5, 3) @default(1.25)
  incomeTaxRate            Decimal  @db.Decimal(5, 4) @default(0.0300)
  socialInsuranceRate      Decimal  @db.Decimal(5, 4) @default(0.1500)
  employmentInsuranceRate  Decimal  @db.Decimal(5, 4) @default(0.0060)
  memo                     String?
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
}

model IncomeTaxBracket {
  id              String   @id @default(cuid())
  fiscalYear      Int
  dependentCount  Int
  minTaxable      Int
  maxTaxable      Int?
  taxAmount       Int
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([fiscalYear, dependentCount, minTaxable])
}
