# Handoff / 引き継ぎ資料 / 交接文档

最終更新 / 最后更新: 2026-07-26

## 1. Project Overview / プロジェクト概要 / 项目概要

| 日本語 | 中文 |
| --- | --- |
| 日本向けの給与管理・SES管理クラウドシステムです。 | 面向日本公司的工资管理和 SES 管理云系统。 |
| Vue.js + Hono + Prisma + PostgreSQL で構築しています。 | 使用 Vue.js + Hono + Prisma + PostgreSQL 构建。 |
| GitHub 管理、Railway 自動デプロイ前提です。 | 代码由 GitHub 管理，Railway 自动部署。 |

作業ディレクトリ / 工作目录:

```text
C:\Users\xuzw7\Documents\Codex\2026-07-01\new-chat\outputs\payroll-cloud-vue
```

## 2. What We Built / 実装済み内容 / 已完成内容

### 2.1 Payroll / 給与管理 / 工资管理

| 日本語 | 中文 |
| --- | --- |
| 社員登録、給与入力、給与保存を実装済み。 | 已实现员工注册、工资录入、工资保存。 |
| 社員ごとの前回入力利用を実装済み。 | 已实现按员工使用上一次录入内容。 |
| 社員本人は自分の明細だけ参照可能。 | 员工本人只能查看自己的明细。 |
| 25日以前は前月を初期表示。 | 25号以前默认显示上个月。 |
| 28日以降は前月以前データを通常更新不可、強制更新で保存可能。 | 28号以后前月以前数据默认不能修改，需要强制修改保存。 |
| 基本給、固定残業手当、手当、住民税、寮使用料を入力可能。 | 可录入基本工资、固定加班津贴、津贴、住民税、宿舍使用费。 |
| 社会保険加入有無、社会保険適用金額を入力可能。 | 可录入是否加入社会保险、社会保险适用金额。 |
| 健康・介護保険、厚生年金、雇用保険、子ども・子育て支援金を分けて管理。 | 健康/护理保险、厚生年金、雇用保险、儿童育儿支援金分开管理。 |
| 職位を社員に登録し、空欄時は明細に SE と出力。 | 员工可登记职位，空白时明细默认输出 SE。 |

### 2.2 Income Tax / 所得税 / 所得税

| 日本語 | 中文 |
| --- | --- |
| 所得税表 CSV インポートを実装済み。 | 已实现所得税表 CSV 导入。 |
| CSV形式は `fiscalYear,dependentCount,minTaxable,maxTaxable,taxAmount`。 | CSV 格式为 `fiscalYear,dependentCount,minTaxable,maxTaxable,taxAmount`。 |
| 年度、扶養人数、課税対象金額で自動查表。 | 按年度、扶养人数、课税对象金额自动查表。 |
| 扶養人数が反映されない不具合を修正済み。 | 已修正扶养人数没有参与计算的问题。 |

### 2.3 Social Insurance / 社会保険 / 社会保险

| 日本語 | 中文 |
| --- | --- |
| 年度料率管理を実装済み。 | 已实现年度费率管理。 |
| 料率入力は小数点6桁まで対応。 | 费率输入支持小数点 6 位。 |
| 健康・介護、厚生年金、雇用保険、子育て支援金を分離。 | 健康/护理、厚生年金、雇用保险、育儿支援金分离管理。 |
| 社員ごとに雇用保険適用を選択可能。 | 可按员工选择是否适用雇用保险。 |

### 2.4 Bonus / 賞与管理 / 奖金管理

| 日本語 | 中文 |
| --- | --- |
| 月給とは別に賞与登録を実装済み。 | 已实现工资之外的奖金录入。 |
| 賞与明細 PDF、賞与 CSV を実装済み。 | 已实现奖金明细 PDF 和奖金 CSV。 |
| 4月末、10月末を通常賞与月として表示。 | 4月末、10月末作为通常奖金月份提示。 |
| 給与明細一括出力に賞与を含めるチェックボックスを追加済み。 | 工资明细批量输出中已添加是否包含奖金的 checkbox。 |
| 賞与の所得税は現状、給与と同じ所得税率ロジック。 | 奖金所得税目前按工资相同所得税率逻辑处理。 |

### 2.5 PDF / 明細PDF / 明细 PDF

| 日本語 | 中文 |
| --- | --- |
| 給与明細 PDF ダウンロードを実装済み。 | 已实现工资明细 PDF 下载。 |
| 指定月範囲の一括ダウンロードを実装済み。 | 已实现指定月份范围批量下载。 |
| ZIP または 1つのPDFにまとめる出力を選択可能。 | 可选择 ZIP 或合并为一个 PDF。 |
| ファイル名は `給与明細_年月_名前`。 | 文件名为 `給与明細_年月_名前`。 |
| 明細レイアウト、支給日溢れ、文字化けを修正済み。 | 已修正明细布局、支给日溢出、文字乱码。 |

### 2.6 Mail / メール / 邮件

| 日本語 | 中文 |
| --- | --- |
| SMTP 送信を試したが Railway から既存SMTPへ接続タイムアウト。 | 已尝试 SMTP 发送，但 Railway 到现有 SMTP 连接超时。 |
| 465/587、SSL設定でも解決できなかった。 | 465/587 和 SSL 设置下仍未解决。 |
| SendGrid 利用案を検討済み。 | 已讨论使用 SendGrid。 |
| 現状は PDF ダウンロードを優先。 | 当前优先使用 PDF 下载。 |

### 2.7 SES Management / SES管理 / SES 管理

| 日本語 | 中文 |
| --- | --- |
| SES 管理をメインメニューに追加済み。 | 已添加 SES 管理主菜单。 |
| 子メニューは取引先、契約、請求、外注費、年間売上、マスタ、個人別利益。 | 子菜单包括取引先、合同、请款、外注费、年度売上、主数据、个人别利润。 |
| 取引先登録、個人事業主フラグ、外部従業員登録を実装済み。 | 已实现取引先注册、个人事业主标记、外部员工注册。 |
| 契約は請求契約/仕入契約、定額/時間範囲/時給に対応。 | 合同支持請求契約/仕入契約、定额/时间范围/时薪。 |
| メンバーなし契約、メンバー区分「指定しない」に対応。 | 支持无成员合同和成员区分“指定しない”。 |
| 一時取引先は取引先登録せず、契約に手入力名を保存可能。 | 临时取引先无需注册，可在合同中手动保存名称。 |
| 請求書生成、重複作成防止、請求書番号自動採番を実装済み。 | 已实现请款书生成、防止重复生成、请款编号自动生成。 |
| 外注費登録、登録済み表示、税抜/税込計算を実装済み。 | 已实现外注费录入、已登记显示、税抜/税込计算。 |
| 年間売上、支出、差額を表示。 | 显示年度売上、支出、差额。 |

### 2.8 UI / CSS

| 日本語 | 中文 |
| --- | --- |
| 青い見出しパネル形式に全体調整。 | 已调整为蓝色标题栏面板风格。 |
| 折りたたみ不具合を修正済み。 | 已修正折叠按钮问题。 |
| 年月、ボタン名、スマホ表示の崩れを修正済み。 | 已修正年月、按钮名、手机端显示异常。 |
| ログイン画面はロゴとシステム名を2行表示。 | 登录页显示 LOGO 和系统名两行。 |
| 外注費の税込/税抜ラベル崩れを修正済み。 | 已修正外注费税区分标签布局问题。 |

## 3. Permission Management / 権限管理 / 权限管理

| 日本語 | 中文 |
| --- | --- |
| ユーザー管理と権限管理を別メニュー化済み。 | 用户管理和权限管理已分成独立菜单。 |
| 権限はロール単位とユーザー個別単位で設定可能。 | 权限可以按角色设置，也可以按用户单独设置。 |
| ユーザー個別権限はロール権限より優先。 | 用户个人权限优先于角色权限。 |
| チェックボックスで表示、閲覧、更新、全件を設定。 | 用 checkbox 设置显示、查看、更新、全件。 |
| 対象は親メニューと子メニュー。 | 权限对象包括父菜单和子菜单。 |
| ユーザー登録/更新時、該当ロールの利用可能権限を初期表示。 | 用户注册/更新时，初始显示该角色可用权限。 |
| SES 子メニューは権限に応じて表示/非表示。 | SES 子菜单根据权限显示/隐藏。 |

現在の権限対象 / 当前权限对象:

| Key | 日本語 | 中文 |
| --- | --- | --- |
| PAYROLL | 給与管理 | 工资管理 |
| PAYROLL_EMPLOYEES | 社員一覧 | 员工列表 |
| PAYROLL_INPUT | 給与入力 | 工资录入 |
| BONUS_INPUT | 賞与入力 | 奖金录入 |
| RATES | 年度料率 | 年度费率 |
| TAX_IMPORT | 所得税表インポート | 所得税表导入 |
| PAYSLIP | 給与明細 | 工资明细 |
| BONUS_SLIP | 賞与明細 | 奖金明细 |
| SES | SES管理 | SES 管理 |
| SES_CUSTOMERS | 取引先管理 | 取引先管理 |
| SES_PROJECTS | 案件・契約管理 | 案件/合同管理 |
| SES_INVOICES | 請求管理 | 请款管理 |
| SES_PARTNER_COSTS | 外注費入力 | 外注费录入 |
| SES_REVENUE | 年間売上 | 年度売上 |
| SES_MASTERS | マスタ管理 | 主数据管理 |
| SES_PROFIT | 個人別利益 | 个人别利润 |
| USERS | ユーザー管理 | 用户管理 |
| PERMISSIONS | 権限管理 | 权限管理 |

## 4. Current Status / 現在の状態 / 当前状态

| 日本語 | 中文 |
| --- | --- |
| 最新リリース済みコミットは `072bce3 Add user-specific menu permissions`。 | 最新已发布 commit 是 `072bce3 Add user-specific menu permissions`。 |
| GitHub `main` へ push 済み。 | 已 push 到 GitHub `main`。 |
| Railway は GitHub 連携で自動デプロイ。 | Railway 通过 GitHub 自动部署。 |
| 作業ツリーは clean。 | 工作区 clean。 |
| `npm run build` 成功済み。 | `npm run build` 已成功。 |

重要な直近コミット / 重要近期 commit:

```text
072bce3 Add user-specific menu permissions
79d7c53 Add role menu permission settings
60d7077 Fix partner cost status chip layout
15098f2 Allow one-time SES contract customers
afc688d Show tax mode on partner cost rows
```

## 5. Where We Are Blocked / 現在の課題 / 当前卡点

| 日本語 | 中文 |
| --- | --- |
| 機能はユーザー個別権限まで実装済みで、リリース済み。 | 功能已做到用户个人权限，并已 release。 |
| 次の課題は API レベルの子メニュー別権限制御を完全にすること。 | 下一步卡点是把 API 级别也完全按子菜单权限细分。 |
| 現状、画面側の表示制御はかなり進んでいる。 | 当前画面层显示控制已经基本完成。 |
| API側は給与/SESの大枠制御と一部制御まで。 | API 层目前是工资/SES 大范围控制和部分控制。 |
| 管理者ロックアウト防止は未実装。 | 防止管理员把自己锁住的保护还没做。 |

未完成ポイント / 未完成点:

| 日本語 | 中文 |
| --- | --- |
| SES API を `SES_CUSTOMERS`、`SES_PROJECTS`、`SES_INVOICES` などに完全分離する。 | SES API 需要完全按 `SES_CUSTOMERS`、`SES_PROJECTS`、`SES_INVOICES` 等拆分权限。 |
| 給与 API を `PAYROLL_INPUT`、`BONUS_INPUT`、`RATES`、`TAX_IMPORT`、`PAYSLIP` に分離する。 | 工资 API 需要按 `PAYROLL_INPUT`、`BONUS_INPUT`、`RATES`、`TAX_IMPORT`、`PAYSLIP` 拆分权限。 |
| 最後の管理者停止禁止、自分の権限管理権限削除禁止が必要。 | 需要禁止停用最后一个管理员，禁止删除自己的权限管理权限。 |
| ユーザー個別権限の「ロールに戻す」機能は未実装。 | 用户个人权限还没有“恢复为角色权限”功能。 |

## 6. Next Steps / 次にやること / 接下来准备做什么

### 6.1 API Permission Mapping / API権限対応表 / API 权限对应表

| 権限キー / 权限 key | 日本語 API対象 | 中文 API 对象 |
| --- | --- | --- |
| PAYROLL_EMPLOYEES | 社員API | 员工 API |
| PAYROLL_INPUT | 給与保存、給与取得、前回入力 | 工资保存、工资查询、上次录入 |
| BONUS_INPUT | 賞与保存、賞与取得 | 奖金保存、奖金查询 |
| RATES | 年度料率、設定 | 年度费率、设置 |
| TAX_IMPORT | 所得税表インポート | 所得税表导入 |
| PAYSLIP | 給与明細PDF | 工资明细 PDF |
| BONUS_SLIP | 賞与明細PDF | 奖金明细 PDF |
| SES_CUSTOMERS | 取引先、外部従業員 | 取引先、外部员工 |
| SES_PROJECTS | 契約 | 合同 |
| SES_INVOICES | 請求書 | 请款书 |
| SES_PARTNER_COSTS | 外注費 | 外注费 |
| SES_REVENUE | 売上、支出、利益 | 売上、支出、利润 |
| SES_MASTERS | 自社情報 | 本公司信息 |
| SES_PROFIT | 個人別利益 | 个人别利润 |

### 6.2 Security Guard / 安全ガード / 安全保护

| 日本語 | 中文 |
| --- | --- |
| 最後の管理者を停止できないようにする。 | 禁止停用最后一个管理员。 |
| 自分自身の `PERMISSIONS` 権限を外せないようにする。 | 禁止取消自己的 `PERMISSIONS` 权限。 |
| 権限変更後は再ログイン推奨または `/me` 再取得。 | 权限修改后建议重新登录或重新获取 `/me`。 |

### 6.3 UI Improvements / 画面改善 / 画面改善

| 日本語 | 中文 |
| --- | --- |
| 権限管理画面を親メニューごとにグループ化。 | 权限管理画面按父菜单分组。 |
| 子メニューをインデント表示。 | 子菜单缩进显示。 |
| ロール権限とユーザー個別権限の差分表示。 | 显示角色权限和用户个人权限的差异。 |
| 「ロール権限に戻す」ボタン追加。 | 添加“恢复为角色权限”按钮。 |

## 7. Release / リリース手順 / 发布步骤

```powershell
cd C:\Users\xuzw7\Documents\Codex\2026-07-01\new-chat\outputs\payroll-cloud-vue
cmd /c npm run build
git status --short --branch
git add <changed files>
git commit -m "<message>"
git push
```

## 8. Important Files / 重要ファイル / 重要文件

| File | 日本語 | 中文 |
| --- | --- | --- |
| `src/App.vue` | 主画面、給与、ユーザー管理、権限管理、主メニュー制御 | 主画面、工资、用户管理、权限管理、主菜单控制 |
| `src/components/SesManagement.vue` | SES管理、SES子メニュー、子メニュー表示制御 | SES 管理、SES 子菜单、子菜单显示控制 |
| `src/server/index.ts` | API、ログイン、権限取得/保存、権限チェック | API、登录、权限读取/保存、权限检查 |
| `prisma/schema.prisma` | DBスキーマ、PermissionMenu、RolePermission、UserPermission | DB schema、PermissionMenu、RolePermission、UserPermission |
| `prisma/migrations/` | DB migration | DB migration |

## 9. Notes / 注意事項 / 注意事项

| 日本語 | 中文 |
| --- | --- |
| `.env`、パスワード、token は漏らさない。 | 不要泄露 `.env`、密码、token。 |
| 勝手に `git reset` しない。 | 不要随便执行 `git reset`。 |
| PowerShell で日本語が文字化けしても、ファイル自体が壊れているとは限らない。 | PowerShell 显示日文乱码时，不一定是文件坏了。 |
| 日本語の多い Vue ファイルは `apply_patch` で編集する。 | 日文较多的 Vue 文件尽量用 `apply_patch` 修改。 |
| 本番では Railway migration 適用状況を確認する。 | 本番环境需要确认 Railway migration 是否成功执行。 |

## 10. Release Update 2026-07-26 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| 権限管理をユーザ管理と同じ構成に整理しました。ロール一覧、ロール追加、非表示、ロールごとの権限設定を同一画面で管理します。 | 权限管理已调整为与用户管理类似的结构。支持角色列表、角色新增、隐藏、按角色设置权限。 |
| 親メニューと子メニューを分けて表示し、子メニュー単位で表示、閲覧、更新、全件、自分のみを設定できます。 | 父菜单和子菜单分开显示，可按子菜单设置显示、查看、更新、全件、仅本人。 |
| ユーザ登録、更新時に権限ロールを選択でき、必要に応じてユーザ個別権限も設定できます。 | 用户新增和更新时可以选择权限角色，也可以按用户单独设置菜单权限。 |
| マニュアルを給与管理とSES管理に分け、日本語版と中文版を別ファイルで追加しました。 | 手册已按工资管理和SES管理拆分，并分别追加日文版和中文版。 |

Manual files / マニュアルファイル / 手册文件:

- `docs/manual_payroll_ja.md`
- `docs/manual_payroll_zh.md`
- `docs/manual_ses_ja.md`
- `docs/manual_ses_zh.md`

## 11. Release Update 2026-07-26-2 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| ロール保存時にAPIエラーが発生した場合、操作画面のメッセージ欄へエラー内容を表示するようにしました。 | 角色保存发生API错误时，错误内容会显示在当前操作画面的消息区域。 |
| 権限設定に、給与管理、SES管理、管理メニュー単位の一括チェック行を追加しました。 | 权限设置追加按工资管理、SES管理、管理菜单为单位的一键勾选行。 |
| 指定画像を `public/favicon.png` に配置し、ブラウザのファビコンとして参照するようにしました。 | 已将指定图片配置为 `public/favicon.png`，并作为浏览器 favicon 引用。 |

## 12. Release Update 2026-07-26-3 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| ロール保存、権限保存、ロール非表示のメッセージ表示先を、全体メッセージ欄から権限管理画面内の専用メッセージ欄へ変更しました。 | 角色保存、权限保存、角色隐藏的消息显示位置已从全局消息区域改为权限管理画面内的专用消息区域。 |

## 13. Release Update 2026-07-26-4 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| 権限管理の一括チェック行と明細行の列幅を揃え、チェックボックスの位置ずれを修正しました。 | 已统一权限管理中一键勾选行和明细行的列宽，修正 checkbox 位置偏移。 |
| 権限一覧内の「メニュー・区分」ヘッダー行を非表示にし、表示内容の重複を減らしました。 | 已隐藏权限列表中的“菜单・区分”标题行，减少重复内容。 |

## 14. Release Update 2026-07-26-5 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| ファビコンがブラウザキャッシュで更新されない問題に対応するため、`favicon.png` の参照URLにバージョンを付与し、`shortcut icon` と `apple-touch-icon` も追加しました。 | 为解决浏览器缓存导致 favicon 不更新的问题，已给 `favicon.png` 引用URL追加版本号，并追加 `shortcut icon` 与 `apple-touch-icon`。 |

## 15. Release Update 2026-07-26-6 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| Chromeが `/favicon.ico` を優先するケースに対応するため、`public/favicon.ico` を実ICO形式で生成し、HTMLの先頭で参照するようにしました。 | 为兼容 Chrome 优先读取 `/favicon.ico` 的情况，已生成真正 ICO 格式的 `public/favicon.ico`，并在 HTML 中优先引用。 |

## 16. Release Update 2026-07-26-7 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| `CompanySetting.systemName` を追加し、マスタ管理の自社情報からシステム名を設定できるようにしました。 | 已追加 `CompanySetting.systemName`，可以在主数据管理的自社信息中设置系统名称。 |
| ログイン画面とヘッダーの「給与管理クラウド」は、公開設定API `/api/public-settings` から取得した値を表示します。 | 登录画面和页眉中的“給与管理クラウド”改为显示公开设置 API `/api/public-settings` 取得的值。 |

## 17. Release Update 2026-07-26-8 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| ファビコンが直接URLで開けなかった原因は、Honoサーバーが `/favicon.ico` と `/favicon.png` を静的配信していなかったためです。 | favicon 直接 URL 打不开的原因是 Hono 服务端没有静态发布 `/favicon.ico` 和 `/favicon.png`。 |
| `src/server/index.ts` に `/favicon.ico` と `/favicon.png` の明示的な `serveStatic` 設定を追加しました。 | 已在 `src/server/index.ts` 中追加 `/favicon.ico` 和 `/favicon.png` 的显式 `serveStatic` 设置。 |

## 18. Release Update 2026-07-26-9 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| ユーザー管理の個別権限表に「ユーザー個別権限設定」と「対象ユーザー」のタイトル帯を追加し、表だけ見えても何の権限か分かるようにしました。 | 用户管理的个别权限表追加“用户个别权限设置”和“对象用户”标题区域，避免只看到表格时不清楚内容。 |

## 19. Release Update 2026-07-26-10 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| 権限チェックボックスの列タイトルとして「表示・閲覧・更新・全件」を再表示しました。「メニュー・区分」は空欄にして、重複感を避けています。 | 权限 checkbox 列标题重新显示“显示・查看・更新・全件”。“菜单・区分”列保持空白，避免重复。 |

## 20. Release Update 2026-07-26-11 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| 操作結果メッセージをログイン画面から分離し、ログイン後の操作画面メニュー直下に表示するようにしました。 | 已将操作结果消息从登录画面分离，改为显示在登录后的操作画面菜单下方。 |
| ログイン画面にはログイン専用メッセージだけ表示するため、前回操作結果がログイン画面に残らないようにしました。 | 登录画面只显示登录专用消息，避免上一次操作结果残留在登录画面。 |

## 21. Release Update 2026-07-26-12 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| 権限管理画面のチェックボックスタイトル行は、以前の削除指示どおり再度削除しました。ユーザー個別権限側のタイトル行は判別用に残しています。 | 权限管理画面的 checkbox 标题行按之前删除要求再次移除。用户个别权限侧的标题行为便于识别仍保留。 |

## 22. Release Update 2026-07-26-13 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| エラーメッセージを赤字で対象操作画面のメニュー直下に表示する方針へ統一しました。給与管理、権限管理、SES各サブメニューで画面外やログイン画面へ残らないようにしています。 | 已统一错误信息显示方式：红字显示在对应操作画面的菜单下方。工资管理、权限管理、SES各子菜单的错误不会残留到登录画面或其他画面。 |
| 操作完了メッセージは緑色のtoastとして画面右上に表示し、自動で消えるようにしました。 | 操作完成消息改为绿色 toast，显示在画面右上并自动消失。 |
| メインメニュー切替時とSESサブメニュー切替時に、前画面のメッセージをクリアするようにしました。 | 切换主菜单或 SES 子菜单时，会清除前一个画面的消息。 |

## 23. Release Update 2026-07-26-14 / リリース更新 / 发布更新

| 日本語 | 中文 |
| --- | --- |
| 給与/SESの日本語・中国語マニュアルを1つのExcelブック `docs/manual_payroll_ses_ja_zh.xlsx` にまとめました。シートは `目次`、`給与_JA`、`給与_ZH`、`SES_JA`、`SES_ZH` です。 | 已将工资/SES 的日语、中文手册整理为一个 Excel 文件 `docs/manual_payroll_ses_ja_zh.xlsx`。工作表为 `目次`、`給与_JA`、`給与_ZH`、`SES_JA`、`SES_ZH`。 |
